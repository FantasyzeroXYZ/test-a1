
export const SUPPORTED_AUDIO_TYPES = "audio/*, .m4b, .mp3, .m4a, .wav";
export const SUPPORTED_SUBTITLE_TYPES = ".srt, .lrc, .vtt, .ass";

// Mock data if needed, but we rely on file uploads
export const MOCK_SUBTITLES = [];

export const DEFAULT_KEY_BINDINGS = {
  playPause: 'Space',
  rewind: 'ArrowLeft',
  forward: 'ArrowRight',
  toggleSidebar: 'KeyS',
  toggleSubtitleMode: 'KeyD',
};
